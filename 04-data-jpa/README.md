# Data JPA

Ma'lumotlar bazasi bilan ishlash — har bir Spring Boot ilovasining asosi.

## Mavzular

`01-jpa-vs-hibernate.md` — JPA (standart) va Hibernate (implementatsiya) farqi.

`02-entity-mapping.md` — `@Entity`, `@Table`, `@Column`, ID strategiyalari, `@PrePersist`.

`03-relationships.md` — OneToOne, OneToMany, ManyToMany, Fetch Type, N+1 muammosi.

`04-spring-data-repository.md` — `JpaRepository`, query metodlari, `@Query`, Pagination.

`05-jpql-native-query.md` — JPQL vs Native SQL, JOIN FETCH, DTO Projection, Criteria API.

`06-transactions.md` — `@Transactional`, `readOnly`, Propagation, Isolation, Optimistic Lock.
