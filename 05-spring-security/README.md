# Spring Security

Autentifikatsiya va avtorizatsiya — ilovangizni himoya qilish.

## Mavzular

`01-security-basics.md` — Authentication vs Authorization, Filter Chain, SecurityContext.

`02-security-config.md` — `SecurityFilterChain`, URL ruxsatlar, CORS, CSRF, PasswordEncoder.

`03-jwt-auth.md` — JWT tuzilishi, `JwtService`, `JwtAuthenticationFilter`, Refresh Token.

`04-user-details-service.md` — `UserDetails`, `UserDetailsService`, `DaoAuthenticationProvider`.

`05-method-security.md` — `@PreAuthorize`, SpEL ifodalari, bean chaqiruv, `@PostAuthorize`.
